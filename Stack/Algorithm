Let stk be an array with size 10

#Pushing an element into the stack

1. Set Initial top=-1
2. If top=9
    (a)Display "Stack Overflow"
    (b)Exit
3. Else
    (a)Enter the data to push and assign the variable stk[top]
    (b)